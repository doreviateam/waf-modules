<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Catégories de sécurité -->
        <record id="module_category_equipment" model="ir.module.category">
            <field name="name">Équipements</field>
            <field name="description">Gestion des équipements et pièces détachées</field>
            <field name="sequence">20</field>
        </record>

        <!-- Groupes -->
        <record id="group_equipment_user" model="res.groups">
            <field name="name">Utilisateur</field>
            <field name="category_id" ref="module_category_equipment"/>
            <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
        </record>

        <record id="group_equipment_manager" model="res.groups">
            <field name="name">Responsable</field>
            <field name="category_id" ref="module_category_equipment"/>
            <field name="implied_ids" eval="[(4, ref('group_equipment_user'))]"/>
            <field name="users" eval="[(4, ref('base.user_root')), (4, ref('base.user_admin'))]"/>
        </record>

        <!-- Règles de sécurité pour les équipements -->
        <record id="equipment_rule_user" model="ir.rule">
            <field name="name">Utilisateur: voir ses équipements</field>
            <field name="model_id" ref="model_equipment"/>
            <field name="domain_force">[('company_id', '=', user.company_id.id)]</field>
            <field name="groups" eval="[(4, ref('group_equipment_user'))]"/>
        </record>

        <record id="equipment_rule_manager" model="ir.rule">
            <field name="name">Responsable: voir tous les équipements</field>
            <field name="model_id" ref="model_equipment"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('group_equipment_manager'))]"/>
        </record>

        <!-- Règles de sécurité pour les pièces détachées -->
        <record id="spare_part_rule_user" model="ir.rule">
            <field name="name">Utilisateur: voir les pièces de sa société</field>
            <field name="model_id" ref="product.model_product_template"/>
            <field name="domain_force">[('company_id', '=', user.company_id.id)]</field>
            <field name="groups" eval="[(4, ref('group_equipment_user'))]"/>
        </record>

        <record id="spare_part_rule_manager" model="ir.rule">
            <field name="name">Responsable: voir toutes les pièces</field>
            <field name="model_id" ref="product.model_product_template"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('group_equipment_manager'))]"/>
        </record>
    </data>
</odoo> 